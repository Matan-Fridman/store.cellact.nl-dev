{
  "name": "secnum-processor",
  "version": "1.0.0",
  "description": "Ingest canonical events → Firestore buckets + Pub/Sub (handler applies logic)",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=main --port=8080"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "@google-cloud/firestore": "^7.11.0",
    "@google-cloud/pubsub": "^4.11.0",
    "ajv": "^8.17.1"
  }
}
