# store.cellact.nl-dev (Light PBX testing)

Preview / testing copy of the Cellact store for **Light PBX Stripe pay** (`/lightpbx/pay`).

- **Not** production `store.cellact.nl`
- Uses staging GCP by default (hostname is not `store.cellact.nl`)
- Base44 start URL: `/lightpbx/pay?systemId=…&userId=…&plan=basic|standard|super&success_url=…&cancel_url=…`

Source branch mirrored from `cellact/store.cellact.nl` `feat/lightpbx-stripe-pay`.
